{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS"
  },
  "deploy": {
    "startCommand": "python manage.py migrate && gunicorn myproject.wsgi:application --bind 0.0.0.0:8080 --log-file -",    
    "restartPolicyType": "ON_FAILURE"
  }
}